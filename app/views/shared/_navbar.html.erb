<nav class="navbar sticky-top navbar-expand-lg bg-light">
  <div class="container-fluid">
    <div class="d-flex flex-row mb-3">
      <a class="navbar-brand" href="/">
        <img src="https://static.thenounproject.com/png/20501-200.png" alt="Logo" width="30" height="24" class="d-inline-block align-text-top">
        <strong>SURF RnR</strong>
      </a>
    </div>
    <% unless current_page?(root_path) %>
      <div class="text-center">
        <%= form_with url: surfboards_path, method: :get, local: true, class: 'd-flex gap-2' do %>
          <%= text_field_tag :query, params[:query], class:"form-control string required", placeholder:'Search a location' %>
          <%= button_tag(type: 'submit', class: 'btn btn-success') do %>
            <i class="fa-solid fa-magnifying-glass" style="color: #ffffff;"></i>
          <% end %>
        <% end %>
      </div>
    <% end %>
    <div class="d-flex flex-row-reverse">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if user_signed_in? %>
          <li class="nav-item">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Options
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "+ Create a board", new_surfboard_path, class: "dropdown-item" %></li>
                <li><hr class="dropdown-divider"></li>
                <li><%= link_to "My bookings", my_bookings_path, class: "dropdown-item" %></li>
                <li><%= link_to "Edit my profile", edit_user_registration_path, class: "dropdown-item" %></li>
              </ul>
            </li>
            <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: 'btn btn-primary' %>
          </li>
          <% else %>
          <li class="nav-item">
            <%= link_to "Sign In", new_user_session_path, class: 'btn btn-primary' %>
          </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>
